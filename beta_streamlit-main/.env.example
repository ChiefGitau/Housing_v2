# API Keys - Replace with your actual API keys
OPENAI_API_KEY=
PINECONE_API_KEY=
PINECONE_ENVIRONMENT_REGION=us-east-1

# Application Configuration
EMBEDDING_MODEL=text-embedding-3-small
PINECONE_INDEX_NAME=small-blogs-emmbeddings-index
CHAT_TEMPERATURE=0

# Optional: External API Configuration
LAISA_API_URL=https://your-api-endpoint.com/api/v1/laisa
LAISA_API_URL_LOCAL=http://localhost:8001/api/v1/laisa
LAISA_CONTEXT=You are a helpful assistant specializing in housing-related laws and regulations.

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual API keys and configuration
# 3. Never commit the .env file to version control
# 4. After updating dependencies, run: pip uninstall pinecone-client -y && pip install pinecone>=3.0.0